{{- if .Values.apiUrl }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "client.fullname" . }}-config
  labels:
    {{- include "client.labels" . | nindent 4 }}
data:
  config.js: |
    window.API_URL = "{{ .Values.apiUrl }}";
{{- end }}
