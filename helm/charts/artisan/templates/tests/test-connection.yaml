apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "artisan.fullname" . }}-test-connection"
  labels:
    {{- include "artisan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "artisan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
